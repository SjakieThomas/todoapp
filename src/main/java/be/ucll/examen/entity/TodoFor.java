package be.ucll.examen.entity;

// Definieer een enum voor verschillende categorieën van taken.
public enum TodoFor {
    WERK,       // Categorie voor werkgerelateerde taken
    SCHOOL,     // Categorie voor schoolgerelateerde taken
    PRIVÉ,      // Categorie voor persoonlijke taken
    ANDEREN     // Voeg andere categorieën toe zoals nodig
}
